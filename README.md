### Este script prepara um ambiente aws com servidores em alta disponibilidade com terraform.

Forma de Uso:

```shell
$ export AWS_ACCESS_KEY_ID="anaccesskey"
$ export AWS_SECRET_ACCESS_KEY="asecretkey"
$ terraform plan
```